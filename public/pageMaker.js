import {
	test,
	makeHeader,
	makeNavBar,
	makeFooter,
	makeEntry,
	createLink,
	createP,
	createImg,
	createElement,
} from "../public/script.js";

// on load, ensure NavBar and Footer are created
makeHeader();
makeNavBar();
makeFooter();
